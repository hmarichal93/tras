name: tras
channels:
  - conda-forge
  - defaults
dependencies:
  # --- Base ---
  - python=3.10
  - pip
  - numpy

  # --- Build tools ---
  - make
  - cmake
  - pkg-config
  
  # --- Compilers (Linux/macOS - Windows users: see README) ---
  - compilers
  
  # --- System libraries ---
  - geos  # Required by shapely 1.7.0

  # --- Optional developer tools ---
  - git
  - jupyterlab
  - matplotlib

  # --- pip packages ---
  - pip:
      - tqdm
      - imageio